<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.eDrink24.config.CustomerMapper">

    <insert id="saveCustomer" parameterType="CustomerDTO">
        insert into CUSTOMER ( loginId, pw, userName, birthdate, gender, phoneNum, email, postalCode, address1, address2)
        values (#{loginId}, #{pw}, #{userName}, #{birthdate}, #{gender}, #{phoneNum}, #{email}, #{postalCode}, #{address1}, #{address2})
    </insert>

</mapper>